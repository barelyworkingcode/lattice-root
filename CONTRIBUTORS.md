# Contributors

Everyone who has turned the crank on this node. Add yourself in the **same PR** as your
contribution:

`- <name or handle> — <what you added> — <made-with> — <node you built for>`

where `<made-with>` is the agent + model that built it — e.g. `Claude Code (Opus 4.8)` — or
`human`.

- lattice — seeded the genesis node and the first toy (`toys/fortune.sh`) — Claude Code (Opus 4.8) — lattice-root
- barelyworkingcode — Snake game (canvas, green/red food mechanics) — Claude Code (Opus 4.8) — lattice-snake